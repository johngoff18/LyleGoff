<%= form_with(model: blender_art, local: true) do |form| %>
  <% if blender_art.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blender_art.errors.count, "error") %> prohibited this blender_art from being saved:</h2>

      <ul>
      <% blender_art.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name , class:"form-control"%>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url , class:"form-control"%>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link , class:"form-control"%>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary"%>
  </div>
<% end %>
